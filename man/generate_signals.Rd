% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signal_generation.R
\name{generate_signals}
\alias{generate_signals}
\title{Generate Signals from Graph with External Regressors}
\usage{
generate_signals(
  graph,
  num_signals,
  beta,
  P = NULL,
  mu = 0,
  sigma = 0.5,
  regressor_mean = 0,
  regressor_sd = 10,
  seed = NULL
)
}
\arguments{
\item{graph}{An igraph object representing the graph structure}

\item{num_signals}{Integer, number of signal observations to generate}

\item{beta}{Numeric vector of regression coefficients (including intercept)}

\item{P}{Optional list of regressor matrices. If NULL, will generate random normal regressors.
Each matrix should be (num_nodes x num_signals). The first should be a matrix of 1's for intercept.}

\item{mu}{Mean for generating latent factors (default: 0)}

\item{sigma}{Standard deviation for observation noise (default: 0.5)}

\item{regressor_mean}{Mean for generating random regressors (default: 0)}

\item{regressor_sd}{Standard deviation for generating random regressors (default: 10)}

\item{seed}{Random seed for reproducibility}
}
\value{
A list containing:
  \item{S_clean}{Clean signals without noise (N x num_signals)}
  \item{S_noisy}{Noisy signals (N x num_signals)}
  \item{P}{List of regressor matrices used}
  \item{L}{Normalized Laplacian matrix used}
  \item{beta}{Regression coefficients used}
  \item{U}{Eigenvectors of Laplacian}
  \item{Lambda}{Eigenvalues of Laplacian}
}
\description{
Generates graph signals according to the model: S = Uf + P*beta + noise
where U are eigenvectors of the graph Laplacian, f are latent factors,
P are external regressors, and beta are regression coefficients.
}
\examples{
\dontrun{
library(igraph)
G <- sample_pa(20, m = 1, directed = FALSE)
result <- generate_signals(G, num_signals = 100, beta = c(1, 0.5))
plot(result$S_noisy[,1], main = "First signal observation")
}
}
